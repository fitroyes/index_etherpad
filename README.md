# Index etherpad

Build an index of etherpad from a main etherpad index.

This project is insired by [libreto](https://github.com/zamentur/libreto).

This repository contains implementations:

- A golang server
- A pure js on page app (build in `output.html`).
